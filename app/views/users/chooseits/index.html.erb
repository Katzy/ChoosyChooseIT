
<div class="ui blue vertical middle aligned relaxed fitted segment">
  <div class="ui centered padded grid">

    <div class="row"></div>
    <div class="row"></div>
      <div class="row">
        <div class="eight wide column">
          <div class="ui center aligned header"><br>
            <% if @user == guest_user %>
              <% if @user.chooseits.length >= 5 %>
                <a href=<%= new_user_registration_path %>><button class="ui huge giant blue button">Create a ChooseIT</button></a>
              <% else %>
                <a href=<%= new_chooseit_path %>><button class="ui huge giant blue button">Create a ChooseIT</button></a>
              <% end %>
            <% else %>

            <% end %>
          </div>
        </div>
      </div>
      <div class="row">
        <% if current_user != nil %>
          <a href=<%= new_chooseit_path %>><button class="ui huge giant blue button">Create a ChooseIT</button></a>
        <% end %>
      </div>
    <div class="row"></div>
    <div class="row"></div>
  </div>
</div>
  <tr>User Id</tr>
  <tr>Title</tr>
  <tr>Link</tr>
  <tr>Genre</tr>
  <tr>Edit</tr>
  <tr>Delete</tr>
</thead>

<tbody>
  <% @chooseits.each do |chooseit| %><br>
    <% @chooseit_choice_1 = chooseit.chooseit_choices[0] %>
    <% @chooseit_choice_2 = chooseit.chooseit_choices[1] %>
    <%= render partial: '/users/chooseits/table_row', locals: { chooseit: chooseit, chooseit_choice_1: @chooseit_choice_1, chooseit_choice_2: @chooseit_choice_2 } %> <br>
  <% end %>
</tbody>

<div class="button"><br><br>
    <%= link_to "Create a ChooseIT", new_chooseit_path %>
</div>
